Read the scenario from the `.spec.md` file.

Use Playwright MCP to run the scenario in a browser and observe the page.

Generate or update a Playwright test in the matching `.spec.ts` file.  
All Playwright imports must come from `@ubs.hive.toolchain/playwright`.