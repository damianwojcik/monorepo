Read the scenario from the `.spec.md` file.

Use Playwright MCP to execute the scenario in a browser, observe the page, and implement it as a Playwright test using `@playwright/test`.

Write or update the corresponding `.spec.ts` file next to the scenario.